apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: reviews
  namespace: 03-canary-deployment
spec:
  parentRefs:
    - name: bookinfo-gateway    
  rules:
    - backendRefs:
        - name: reviews         
          port: 9080
